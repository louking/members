<p>Just a friendly reminder that you have {{ aninvitation_text }} for {{ meeting.purpose }} on {{ meeting.date }}
    {% if meeting.time %}{{ meeting.time }}{% endif %}</p>

{% if meeting.location %}
    <p>Location / URL</p>
    <p style="white-space:pre-wrap; margin-left: 1em;">{{ meeting.location }}</p>
{% endif %}

{{ message }}

{% if rsvp_required or has_status_reports %}
    <p>Please {% if rsvp_required %}RSVP and {% endif %}{% if has_status_reports %}enter your {{ statusreport_text }}{% endif %} by clicking <a href={{ rsvpurl }}>here</a>.</p>
{% endif %}

{% if has_status_reports %}
    {% if positions %}
    <p>This reminder is being sent specifically for the following Position reports, but please note you may have other
    reports outstanding.</p>

    <ul>
    {% for position in positions %}
    <li>{{ position.position }}</li>
    {% endfor %}
    </ul>
    {% endif %}
{% endif %}

{% if show_action_items %}
    {% if actionitems %}
    <p>Your outstanding action items are listed below. You can update the action item status by clicking <a href={{ actionitemurl }}>here</a>.</p>

    <table>
    <tr><th>Action Item</th><th>Status</th></tr>
    {% for actionitem in actionitems %}
    <tr>
    <td>{{ actionitem.action }}</td>
    <td>{{ actionitem.status }}</td>
    </tr>
    {% endfor %}
    </table>

    {% else %}
    <p>You have no outstanding action items.</p>
    {% endif %}
{% endif %}